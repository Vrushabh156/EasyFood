<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@drawable/breakfast"
        android:label="@string/app_name"
        android:roundIcon="@drawable/breakfast"
        android:supportsRtl="true"
        android:theme="@style/Theme.EasyFood"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".ui.activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.BudgetBuddies.BlueStatusBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.CategoryMealsActivity"
            android:exported="false"
            android:theme="@style/Theme.BudgetBuddies.BlueStatusBar" />
        <activity
            android:name=".ui.activities.MealActivity"
            android:exported="false"
            android:theme="@style/Theme.BudgetBuddies.BlueStatusBar" />
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.BudgetBuddies.BlueStatusBar" />
    </application>

</manifest>